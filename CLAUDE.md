# Secco Platform

Monorepo da Secco Platform — ecossistema corporativo modular para .NET. Produtos adotáveis de forma independente via NuGet, com identidade arquitetural única: Clean Architecture, OpenAPI + NSwag + Scalar, multi-tenancy database-per-tenant, SDKs próprios.

## Fonte da verdade

- **`docs/adr/secco-platform-adrs.md`** — ADRs ratificadas. Ler antes de qualquer decisão estrutural. **Nunca produzir código que contradiga uma ADR Aceita.** Decisão arquitetural nova (afeta 2+ produtos ou é difícil de reverter) → propor nova ADR antes de codificar; ADRs nunca são editadas retroativamente, são substituídas.
- **`docs/roadmap.md`** — fase atual e próximos entregáveis. Atualizar os checkboxes ao concluir itens.
- **`docs/design-decisions-log.md`** — histórico de perguntas de design e alternativas descartadas (não versionante — para consultar contexto e candidatos a reavaliação, nunca para contradizer uma ADR).
- **`.claude/skills/`** — regras operacionais detalhadas (`secco-platform-standards`, `secco-db-naming` e `secco-platform-release`). Seguir sempre.

## Estado atual

Fases 0–3 concluídas: ADRs ratificadas, fundação do monorepo, CI, `Secco.SharedKernel` v0.1 (tag `sharedkernel/v0.1.0`), `Secco.SDK.AspNetCore` v0.1 (correlation, tenancy, health checks, resilience e composição `AddSeccoPlatform()`) e `Secco.SDK.EntityFrameworkCore` v0.1 (`SeccoDbContext` + `SeccoNamingConvention` ADR-0017 + seeding ADR-0019). **Fase 4 em andamento — `Secco.LogStream` reescrito do zero** (decisão: sem migração de histórico do RS.Logging; plano 4.1–4.7 no roadmap). **Fase 4 concluída** (4.1–4.7): produto completo — Log geral, processos + auditoria, ApiCallLog, retenção opt-in, PostgreSQL como segundo provider (assemblies de migrations por engine, seleção via `LogStream:Database:Provider`), Docker. Full-text ficou no backlog (LIKE na v1). **Fase 5 concluída**: `dotnet new secco-service` (`templates/secco-service`, monorepo-first com recurso Sample de exemplo; job `validate-template` no CI instancia e testa o produto gerado). **Fase 6 em andamento — `Secco.SecureGate`** (ADR-0022: OpenIddict + Identity, identidade como dado de plataforma). 6.1–6.3 concluídas: banco de plataforma com schema ADR-0017 completo; OpenIddict server com client credentials + JWKS (E2E cross-produto provado); catálogo de tenants via API — `TenantDatabase` por (tenant, produto), gestão com scope `securegate:admin` (write-only para segredos), leitura com scope `catalog:<produto>` (least privilege), `Secco.SecureGate.Client` nasceu com `SecureGateTenantCatalog : ITenantCatalog` (client credentials automático, cache TTL + stale em falha; sem a seção `Secco:SecureGate` o catálogo por configuração segue valendo em DEV), SDK converte exceções de tenancy em ProblemDetails (400 / 503+Retry-After); LogStream adotou — E2E com zero tenants em configuração. **6.4 concluída** (ADR-0021): `AddSeccoAuthorization()` no SDK — policies dinâmicas por permissão (`recurso:acao`, `SeccoPermissions` no kernel; constantes em cada produto por ADR-0003), cache `(tenant_id, role)` TTL curto estrito fail-closed, resolver por configuração (DEV) ou remoto via `Secco.SecureGate.Client` (scope `authorization:read`); clients OIDC têm roles (`ds_roles` — máquinas no mesmo modelo Role+Permission); SecureGate serve gestão de roles/permissões por tenant + resolução; pipeline: tenancy ANTES de authorization; LogStream com permissões em todos os endpoints — revogação propaga em ≤ 1 TTL com token válido. **6.5 concluída** (ADR-0022): login de usuário — **authorization code + PKCE obrigatório** + refresh; ASP.NET Identity (`AddIdentityCore`, cookie não-default — o padrão da API segue JwtBearer) + telas Razor (Login self-contained, antiforgery); endpoints `/connect/authorize|userinfo|logout`, token estendido re-derivando claims do banco a cada emissão (revogação/desativação reflete no refresh); email único global (tenant vem do registro, sem seletor); usuários **provisionados por admin** (`/api/v1/tenants/{id}/users`, scope `securegate:admin`, sem auto-registro); consent implícito para first-party (tela adiada — YAGNI). **Fase 6 concluída** — o quarteto SharedKernel + SDK + LogStream + SecureGate provou o padrão. **Fase 7 em andamento — `Secco.AdminPortal`** (ADR-0023: **Blazor Server** como relying party OIDC; não é produto de 4 camadas — sem domínio/banco próprios; chama os produtos **on-behalf-of** o operador via clients NSwag; operador **cross-tenant** com role `installation-operator` no tenant de plataforma). **7.1 concluída**: fundação Blazor Server + login OIDC (cookie + code/PKCE; access token custodiado como claim no cookie e anexado por chamada via `IOperatorTokenProvider`) + shell + gate `Operator` (`RequireRole("installation-operator")`) + fatia de tenants (`/tenants` via `Secco.SecureGate.Client`); SecureGate ganhou tenant de plataforma + role de operador (seed de referência) e **filtro do scope `securegate:admin` no `/connect/authorize`** (só operadores — login comum não escala, defesa em profundidade ADR-0020), + dev seed do client `secco-adminportal` e operador demo. **7.2 concluída**: administração de identidade — página de detalhe do tenant (drill-in `/tenants/{id}`) com Usuários (listar/criar) e Roles & permissões (listar/criar/editar em texto livre, PUT idempotente); `ISecureGateClientFactory` central anexa o token do operador; erros do client traduzidos (ProblemDetails → mensagem). Reusa os endpoints existentes do SecureGate (gated por `securegate:admin`). **7.3 concluída** (ADR-0024, resolve a questão em aberto da ADR-0023): visualização de logs cross-tenant — o token do operador é **tenant-less** (sem `tenant_id`; escolhe o tenant por requisição via `X-Tenant-Id`, o caminho "sem claim → header" da ADR-0005, sem reformar a regra de conflito) e o papel `installation-operator` recebe um **read-set fixo** (`*:read` de logs) em qualquer tenant via caso especial na resolução do SecureGate (produtos/SDK inalterados); AdminPortal com página `/tenants/{id}/logs` (busca paginada via `Secco.LogStream.Client` on-behalf-of, token + `X-Tenant-Id`). Corrigido de passagem: enum do LogStream sem `type: string` no OpenAPI → partial class no client com `JsonStringEnumConverter`. **7.4 concluída**: gestão de bancos de tenant — seção Bancos na página do tenant (lista os produtos com banco a partir de `Products`; connection strings nunca aparecem, write-only) + cadastrar/rotacionar via o PUT idempotente da 6.3 (produto em texto livre + input password), sem novos endpoints. **Fase 7 concluída** — o AdminPortal cobre tenants, identidade, logs cross-tenant e bancos. A fundação da plataforma está completa (SharedKernel + SDK + LogStream + SecureGate + AdminPortal). **Fase 8 concluída — `Secco.NotificationHub`** (despacho multi-canal de notificações — e-mail assíncrono + inbox in-app; sem motor de templates, sem acoplamento ao SecureGate — o chamador resolve/informa destinatário/usuário; broker de mensageria, ADR-0015 Camada 3, continua adiado). **8.1**: gerado via `dotnet new secco-service`, checklist pós-geração completo (migrations, contrato, CI). **8.2**: domínio real (entidade `Notification`) substitui o recurso Sample; `Secco.SDK.AspNetCore` ganhou `IBackgroundJobScheduler`/`AddSeccoBackgroundJobs()` (ADR-0015 Camada 2, 1º consumidor real) — Hangfire com storage SQL Server no banco de **plataforma** do produto (nunca por tenant), `TenantJobRunner` restaura o tenant no escopo automaticamente antes do job rodar; `POST /api/v1/notifications` valida e enfileira o envio (MailKit), `GET /api/v1/notifications/{id}` consulta status (`Pending`/`Sent`/`Failed`); falha marca `Failed` e relança — quem decide o retry é o Hangfire, sucesso numa tentativa seguinte sobrescreve para `Sent`; endpoints com permissões (`notifications:read`/`write`, ADR-0021). **8.3**: PostgreSQL como segundo provider do banco de tenant (ADR-0018) — Hangfire (banco de plataforma) segue SQL-Server-only no v1, é infraestrutura de fila, não dado de tenant; `docker-compose.yml` ganhou MailHog (SMTP fake) e criação automática do banco de plataforma do Hangfire. **8.4**: canal in-app — entidade `InAppNotification` própria (lido/não lido, sem status de entrega — ciclo de vida diferente do e-mail, entidades separadas); `POST /api/v1/notifications` reformado para despacho multi-canal (`channels: ["email","in_app"]` numa chamada só, reforma aceita por o produto ainda não ter consumidor real); `userId`/`recipient` seguem campos independentes (sem acoplar um ao outro nem ao SecureGate); novos endpoints `GET /api/v1/in-app-notifications` (não lidas), `.../count` e `POST .../{id}/read`, com permissões próprias. A plataforma ganhou `IBackgroundJobScheduler` como capacidade reutilizável para qualquer produto futuro. **Incrementos pós-Fase 8**: **cifragem AES-256-GCM das connection strings de tenant** no catálogo do SecureGate (ADR-0025 — value converter do EF Core, formato versionado `secco-enc:v1:`, chave em `SecureGate:Catalog:EncryptionKey` com fail-fast em Production, legado em claro convergido por seeder idempotente no startup; a chave entra no backup com o mesmo peso do banco); **login federado com Microsoft Entra ID por tenant** (ADR-0026 — federação é SÓ autenticação, o SecureGate segue emissor único e tokens do Entra nunca chegam aos produtos; `TenantFederation` 1:1 com tenant, app registration multi-tenant única da plataforma, usuários **pré-provisionados fail-closed** casando por e-mail apenas dentro do `tid` registrado e depois por `oid` imutável); **ambiente de desenvolvimento no VS Code** (`.vscode/launch.json` com as duas configurações de auth de DEV por API — standalone HS256 e federado RS256 via SecureGate, este o único modo em que o AdminPortal funciona — e `docker-compose.yml` **único na raiz com profiles**; portas: API a partir de 4001, tela a partir de 5001, HTTP = HTTPS + 100). **`Secco.SDK.Testing` entregue** (ADR-0027): `SeccoApiFactory<TProgram>` com `ConfigureWebHost` selado + hooks substitui as 5 cópias de `*ApiFactory`, chave HS256 aleatória por instância (a constante literal sumiu do repositório), instância de SQL Server isolada por suíte com override por `SECCO_TEST_SQLSERVER`; na mesma entrega o seletor de provider por receita (`SeccoDatabaseProviders`, sem acoplar o SDK EF Core a engine) e o `BindSection` apagado em favor de `AddOptions<T>().BindConfiguration()` — na Application as options seguem POCO, por adaptador na composição (ADR-0002). **Issues #1 e #2 entregues** (2026-09-03, canal `adopter-demand`): nasce o pacote **`Secco.SDK.Logging`** com o `AddLogStream()` que a ADR-0008 prometia e nunca existiu — `ILoggerProvider` com fila local limitada (descarte contado, nunca bloqueia, nunca lança), lote por tenant e enriquecimento de tenant/correlação/serviço/categoria; o `SeccoAmbientContext` (`AsyncLocal` no SDK, escrito pelos middlewares e pelo `SetTenant`) existe porque os contextos são `Scoped` e um `ILoggerProvider` é singleton, e job do Hangfire não tem `HttpContext`; guarda anti-recursão por categoria é requisito de correção (o dispatcher usa `HttpClient`, que loga); o `SeccoClientCredentialsHandler` subiu do `Secco.SecureGate.Client` para o SDK (OAuth 2 puro, não contrato de produto). O LogStream ganhou `ds_service_name`/`ds_category` filtráveis e `CorrelationId` por item no `/batch` (o header carimbava o lote inteiro). **A trilha de auditoria é recurso do LogStream** (`AuditEntry`), não `Secco.Audit` como produto nem coluna no `LogEntry`: ingestão **síncrona** (`201`) de propósito — registro com obrigação legal não pode sumir numa fila cheia, e a consequência assumida é que LogStream indisponível bloqueia quem audita; retenção passou a ter janelas independentes por classe de dado, com auditoria **não expirando por padrão** e cortando pelo `CreatedAt` do servidor, nunca pelo `OccurredAt` declarado (ADR-0020); `audit-entries:read` entrou no read-set do `installation-operator` (ADR-0024). Fora de escopo, registrado: resistência a adulteração da trilha (append-only só por ausência de endpoint de escrita) exige ADR nova. Configuration e FeatureFlags seguem no backlog; **`Audit` saiu dele** — resolvido dentro do LogStream. **Fronteira do console de operação definida** (2026-09-04, issue #4): **cada instalação é soberana** — a Intranet é produto que empresas baixam e rodam por conta, e a multi-tenancy existe para a empresa desenvolver **outros produtos seus** sobre SecureGate e LogStream; não é o portal da Secco, é o portal da empresa adotante, que também pode implantar só o SecureGate ou só o LogStream sem tenancy. O `Secco.AdminPortal` **permanece com papel redefinido**: console mínimo para quem adota **sem** a Intranet — preserva "produtos adotáveis de forma independente"; a Intranet é o portal completo de quem a usa, e a duplicação de telas entre os dois é o preço consciente dessa promessa. A "identidade dupla" da ADR-0024 encolhe: vira hierarquia dentro de uma instalação (admin de instalação × admin de tenant), e o papel passou a significar **operador desta instalação**. Para ler log cross-tenant sem tocar em ADR nenhuma, vale **elevação explícita**: segundo token sem `tenant_id`, escopo só `logstream`, só leitura, TTL curto, sem refresh, descartado ao sair da área — privilégio como ato, não estado (formato inspirado no RFC 8693). A **#3 (provisionamento) sai destravada com dono definido**: pela ADR-0007 do `secco-intranet`, criar database/login/permissão é capacidade **da plataforma** — vive no SecureGate, que já custodia o catálogo cifrado (ADR-0025), nunca no portal. Descartadas as alternativas de aposentar o AdminPortal (os front-ends não são o mesmo stack: Intranet é MVC, AdminPortal é Blazor Server) e de virar CLI. Fronteira ratificada na **ADR-0030** (2026-09-12), escrita quando a área administrativa da Intranet começou — `platform-operator` passou a `installation-operator` (convergência no seed de referência preserva o `Id` do papel e, com ele, as atribuições), e leitura de log cross-tenant a partir de produto tenant-scoped exige **elevação explícita**: segundo token sem `tenant_id`, escopo só `logstream`, leitura, TTL curto, sem refresh, descartado ao sair da área. Aberta a issue #6 — **"acessar como"**, permitido **apenas ao admin do tenant** e estritamente intra-tenant (o operador de instalação não recebe o recurso), com sete regras de escopo inicial e `OnBehalfOfId` no `AuditEntry` como candidato a decidir antes de a trilha ganhar volume. **Issue #3 entregue** (2026-09-05, **ADR-0028**): **provisionamento de banco de tenant** no SecureGate — a plataforma decidiu database-per-tenant na ADR-0005 e nunca teve como criar esses bancos, então o isolamento era **convenção, não garantia** (connection string com `sa` alcança o banco do vizinho). Dois modos com os mesmos artefatos: **script sempre disponível** (o SecureGate gera o SQL, um DBA aplica — muitos DBAs corporativos jamais concedem `dbcreator` a uma aplicação) e **automação opt-in fail-closed**; o script devolvido é exatamente o texto que a execução automática aplica. **Execução síncrona** por decisão: Hangfire moveria *quando* a credencial é usada, não *onde* mora, e repetir DDL automaticamente é mais perigoso que útil. Teto do que se concede: **`db_owner` no próprio banco e nada no servidor**. Identificadores por **allowlist estrita antes de qualquer concatenação** — DDL não aceita parâmetro para nome de objeto, então essa é a única barreira contra injeção com privilégio de administrador. Segredo gerado no servidor, exibido uma vez no script, persistido só cifrado (ADR-0025); reprovisionar responde **409**. Junto, o **painel de estado dos bancos**, que não usa credencial privilegiada (sonda a conexão de runtime de cada tenant), e o `docker-compose.yml` **deixou de ensinar `sa`** (`docker/sqlserver-init.sql` cria um login por banco com `db_owner` só nele; isolamento verificado à mão no container). Corrigido na revisão: a retenção de auditoria cortava pelo `OccurredAt` **declarado pelo chamador** — agora corta pelo `CreatedAt` do servidor. Fora de escopo: PostgreSQL (abstração pronta), rotação de senha, desprovisionamento, tela no AdminPortal.

## Segurança — critério transversal obrigatório (ADR-0020)

Toda análise de design e toda revisão de código avalia explicitamente, antes de implementar: confiança em input externo (headers, payloads — nunca propagar/persistir sem validar formato/tamanho), injeção (SQL, log forging, header injection), vazamento de informação (erros, logs, stack traces em produção), isolamento de tenant, autenticação/autorização explícitas, negação de serviço (limites de tamanho/taxa em input não confiável), e dependências novas (manutenção ativa, CVEs conhecidas). Ao propor decisões de design, apresentar o risco de segurança de cada opção — não só o trade-off funcional.

## Comandos

```bash
dotnet restore Secco.Platform.slnx
dotnet build Secco.Platform.slnx --configuration Release   # warnings = erros
dotnet test Secco.Platform.slnx
```

## Regras inegociáveis (resumo — detalhes nas skills e ADRs)

- Prefixo `Secco.*` em tudo que é novo; nada novo com `RS.` (ADR-0016).
- Clean Architecture, 4 camadas, dependências apontando para dentro; zero infraestrutura em Domain/Application (ADR-0002).
- Erros de negócio via `Result<T>` do SharedKernel, nunca exceções para fluxo (ADR-0004).
- SharedKernel só admite tipos usados por 2+ produtos, com zero dependências externas, sem I/O, estáveis — na dúvida, fica no produto (ADR-0003).
- Comunicação entre produtos exclusivamente via clients NSwag gerados (`Secco.<Produto>.Client`); nunca `HttpClient` manual; contrato mudou → regenerar `openapi.json` + client no mesmo PR (ADR-0006).
- Multi-tenancy database-per-tenant; nenhuma query cruza tenants; acesso via `ITenantConnectionFactory` (ADR-0005).
- Auth: claims curtos JWT/OIDC (`sub`, `role`, `tenant_id`, `scope`) — nunca `System.Security.Claims.ClaimTypes`; mapeamento automático de claims desligado centralmente em `AddSeccoAuthentication()` (ADR-0007).
- Autorização granular: token só carrega `role`; permissões (`recurso:ação`) resolvidas em runtime via `Secco.SecureGate.Client`, cache `(tenant_id, role)` com TTL curto, **fail-closed** se indisponível — `AddSeccoAuthorization()` (ADR-0021).
- Banco: SQL Server é o provider padrão, PostgreSQL segundo (ADR-0018); notação húngara minúscula — `tb_`, `vw_`, colunas `id_pk_`/`id_fk_`/`id_pfk_`, `ds_`, `dt_`, `nr_`, `ie_`, `fl_`, `vl_`, `qt_` — via convention global do EF Core, nunca digitada à mão (ADR-0017); `sp_` mantido por decisão registrada.
- Seed: referência (todos os ambientes, idempotente) vs desenvolvimento (só DEV, guarda dupla `IsDevelopment()` + flag) — nunca misturar (ADR-0019).
- Background: nativo → Hangfire/SQL Server via `IBackgroundJobScheduler` do SDK → broker só com ADR nova (ADR-0015).
- Testes: xUnit + FluentAssertions **v7 fixado** (v8+ é licença comercial) + NSubstitute + Testcontainers (MsSql padrão); toda feature acompanha testes no mesmo PR (ADR-0012).
- Central Package Management: versões só em `Directory.Packages.props`; `PackageReference` sem `Version` nos csproj (ADR-0011).
- Commits: Conventional Commits (`feat(logstream): ...`).

## Checklist antes de concluir qualquer entrega

Executar o checklist da skill `secco-platform-standards` (ADRs respeitadas, camadas corretas, Result pattern, contratos regenerados, isolamento de tenant, análise de segurança ADR-0020, testes incluídos) e atualizar `docs/roadmap.md` se algum item de fase foi concluído.
